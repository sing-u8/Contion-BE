# 슬라이스 스펙: `<슬라이스 이름>`

> **이 문서의 책임**: Build 루프(`게이트 → 스펙 → 계획 → 격리 → TDD → 리뷰 → 검증 → 통합`)의
> **스펙** 단계 산출물이다. 슬라이스를 코드로 옮기기 전 작은 설계를 문서로 굳혀, 다음
> **계획**(`slice-plan-template.md`) 단계로 넘어갈 근거를 만든다. 이 문서를 쓰기 전
> `slice-gate-checklist.md`를 통과했어야 한다.

---

## Planning Gate 노트 (Docs consulted)

- [ ] `1-requirements.md#<REQ-...>` — `<재독하며 확인한 것>`
- [ ] `2-strategy.md#<영역>` — `<...>`
- [ ] `3-tactics.md#<블록/규칙>` — `<...>`
- [ ] `boundary-catalog.md` — `<관련 혼동 쌍 또는 "해당 없음">`

## 목적 / 배경

`<이 슬라이스가 왜 필요한가. 관련 요구사항 ID를 인용한다: REQ-<영역>-00X>`

## 목표 / 비목표

- 목표: `<이 슬라이스가 끝나면 참이 되는 것>`
- 비목표: `<일부러 다루지 않는 것 — 범위를 좁히는 결정>`

## 설계 개요

`<접근 방식을 짧은 문단이나 목록으로 적는다. 다이어그램이 필요하면 텍스트로 스케치한다.>`

## 주요 결정 & 대안

| 결정 지점 | 선택한 안 | 이유 | 기각된 대안 |
| --- | --- | --- | --- |
| `<결정이 필요했던 지점>` | `<선택한 안>` | `<근거>` | `<기각한 안 + 기각 사유>` |

## 영향받는 경계

| 바운디드 영역 | 영향 | 비고 |
| --- | --- | --- |
| `<영역 이름>` | `<읽기/쓰기/신규 등>` | `<컨텍스트 맵과 일치하는지 확인>` |

## 미해결 질문

- [ ] `<결정이 필요한데 아직 못 내린 것>`

## 완료 정의 (Definition of Done)

- [ ] 수용 기준(`REQ-<영역>-00X`)을 충족한다.
- [ ] 관련 규칙(불변식)을 위반하지 않는다: `<INV-...>`
- [ ] `<검증 방법 — 실행할 명령/절차>`

## 다음 단계

이 스펙을 승인하면 → `slice-plan-template.md`로 단계별 구현 계획을 쓴다.
