# 사례 연구: `<슬라이스/사건 이름>`

> **이 문서의 책임**: 슬라이스 하나(또는 사건 하나)를 **무엇이 · 왜 · 수정 · 교훈** 네
> 부분으로 회고해 학습 루프에 공급한다. 개별 사례 연구는 그 자체로 끝나지 않고, 아래
> "피드백 엣지" 절을 통해 SSoT(`docs-skeleton/`)나 플레이북으로 반영돼야 루프가 닫힌다.

---

## 메타데이터

- 슬라이스/사건: `<이름>`
- 날짜: `<YYYY-MM-DD>`
- 관련 요구사항: `<REQ-...>`
- 심각도: `<정보 / 경미 / 중대>`

## 무엇이 (현상)

`<무엇이 예상과 다르게 동작했는지, 관찰된 사실만 적는다 — 아직 원인 해석은 넣지 않는다.>`

- `<증상 1>`
- `<증상 2>`

## 왜 (원인)

`<근본 원인. "증상"과 "원인"을 섞지 않는다 — 증상은 위 절에, 원인은 여기.>`

- `<직접 원인>`
- `<근본 원인(직접 원인과 다르다면 구분해서)>`

## 수정 (조치)

| 조치 | 대상 파일/문서 | 커밋/참조 |
| --- | --- | --- |
| `<무엇을 고쳤는지>` | `<경로>` | `<커밋 해시 또는 링크>` |

## 교훈 (일반화)

`<이번 한 번에만 해당하지 않도록, 재사용 가능한 문장으로 적는다.>`

- `<교훈 1 — 다음에 같은 실수를 막으려면 무엇을 다르게 할지>`

## 피드백 엣지 (SSoT/플레이북 반영)

이 사례에서 나온 교훈을 어디에 반영했는지 기록한다. 반영하지 않은 교훈은 다음 슬라이스에서
그대로 반복된다.

- [ ] `boundary-catalog.md`에 혼동 쌍으로 추가했다 (해당 시): `<행 식별자>`
- [ ] `3-tactics.md`의 규칙 표에 불변식으로 추가했다 (해당 시): `<규칙 ID>`
- [ ] `review-playbook.md`의 리뷰 차원/승격 절차에 반영했다 (해당 시): `<...>`
- [ ] 지속 메모리(세션을 넘어 사는 저장소)에 기록했다 (해당 시): `<...>`
- [ ] 해당 없음 — 일회성 사건이며 일반화할 규칙이 없다.
