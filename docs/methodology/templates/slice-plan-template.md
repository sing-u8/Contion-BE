# 슬라이스 계획: `<슬라이스 이름>`

> **이 문서의 책임**: Build 루프(`게이트 → 스펙 → 계획 → 격리 → TDD → 리뷰 → 검증 → 통합`)의
> **계획** 단계 산출물이다. 승인된 슬라이스 스펙(`slice-spec-template.md`)을 실행 가능한
> 단계별 계획으로 바꾼다. 이 계획의 각 단계는 격리 → TDD → 리뷰 → 검증 → 통합으로 이어진다.

---

## 참조 스펙

- 스펙 문서: `<slice-spec-template.md로 작성한 경로>`
- 요구사항: `REQ-<영역>-00X`

## Files

- Create: `<새로 만들 경로>`
- Modify: `<고칠 경로>`

## Interfaces

- Consumes: `<이 슬라이스가 전제하는 상위 계약/문서/기존 코드>`
- Produces: `<이 슬라이스가 새로 내놓는 계약/산출물>`

## 격리

- 작업 공간: `<브랜치/작업트리 이름>`
- 통합 대상 브랜치: `<...>`

## 단계

- [ ] **Step 1: `<이름>`** — `<실패하는 테스트부터 — 무엇을 red로 만들 것인가>`
- [ ] **Step 2: `<이름>`** — `<구현 — red를 green으로>`
- [ ] **Step 3: `<이름>`** — `<필요하면 리팩터>`
- [ ] **Step N: 검증**
      Run: `<실행할 명령>`
      Expected: `<기대하는 출력/상태>`
- [ ] **Step N+1: 통합** — `<머지/커밋 방식. 보호 브랜치 직접 커밋 금지.>`

## Self-Review

- [ ] 스펙의 "완료 정의"를 전부 충족했다.
- [ ] `boundary-catalog.md`의 관련 쌍을 위반하지 않았다.
- [ ] 검증 명령을 실제로 실행하고 출력을 읽었다(증거 없이 "완료" 없음).

## 완료 후

- [ ] 비자명한 결과가 있으면 `case-study-template.md`로 사례 연구를 남긴다.
- [ ] 반복되는 리뷰 지적이 있으면 `review-playbook.md`의 승격 절차를 밟는다.
