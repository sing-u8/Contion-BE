# 4. 데이터 설계 — 스키마 · 쿼리 형태

> **이 문서의 책임**: 전술 설계(`3-tactics.md`)의 빌딩블록을 **어떻게 저장하고 조회**하는지
> 정의한다(스키마 + 쿼리 형태). 편집 전 `3-tactics.md`를 같은 세션에서 재독한다.

---

## 게이트 노트 (편집 전 확인)

- [ ] `3-tactics.md`에서 저장 대상 빌딩블록을 재독했다: `<블록 이름>`
- [ ] 관련 규칙(불변식) ID를 확인했다: `<INV-...>`

## 스키마

저장 계층은 라벨된 placeholder로 둔다 — 구체 기술은 채택 프로젝트가 채운다
(예: `<관계형 테이블>`, `<문서 컬렉션>`, `<키-값 저장소>` 등).

### `<저장 단위 이름>` (예: 테이블/컬렉션/집계 하나)

| 필드 | 타입 | 필수 | 규칙(불변식 참조) | 비고 |
| --- | --- | --- | --- | --- |
| `<id>` | `<식별자 타입>` | 예 | `<3-tactics.md의 식별자 절>` | `<생성 방식>` |
| `<필드 이름>` | `<타입>` | `<예/아니오>` | `INV-<영역>-001` | `<...>` |

- 관계/참조: `<이 저장 단위가 참조하는 다른 저장 단위와 관계 종류>`
- 삭제 정책: `<예: 하드 삭제 / 소프트 삭제-표시 / 보존 후 익명화 등>`

## 쿼리 형태

이 데이터가 **어떻게 조회되는지**(접근 패턴)를 먼저 적고, 그 다음에 인덱스/최적화를 붙인다.

| 쿼리 이름 | 목적 | 입력 | 반환 형태 | 예상 빈도/규모 |
| --- | --- | --- | --- | --- |
| `<조회 이름>` | `<무엇을 위해 쓰이는지>` | `<필터/파라미터>` | `<단건/목록/페이지 등>` | `<예: 요청당 1회, 목록 화면 등>` |

## 변경/마이그레이션 정책

- 스키마 변경 시 하위 호환을 어떻게 다루는지: `<...>`
- 되돌리기(rollback) 전략: `<...>`

## 변경 이력

| 버전 | 날짜 | 변경 | 사유 |
| --- | --- | --- | --- |
| `v0.1` | `<YYYY-MM-DD>` | 초안 작성 | `<...>` |
