# 3. 전술 설계 — 빌딩블록 · 규칙 · 이벤트 · 식별자

> **이 문서의 책임**: 각 바운디드 영역(`2-strategy.md`) 내부의 **빌딩블록 · 규칙(불변식) ·
> 이벤트 · 식별자**를 정의해 도메인 모델을 만든다. 편집 전 `2-strategy.md`를 같은 세션에서
> 재독한다.

---

## 게이트 노트 (편집 전 확인)

- [ ] `2-strategy.md`에서 이 전술 설계가 속한 바운디드 영역을 재독했다: `<영역 이름>`
- [ ] 관련 요구사항 ID를 확인했다: `<REQ-...>`

## 영역: `<영역 A>`

### 빌딩블록

| 이름 | 종류 | 책임 | 소유 규칙(불변식) |
| --- | --- | --- | --- |
| `<블록 이름>` | `<예: 고유 정체성을 갖는 핵심 단위 / 정체성 없는 부속 값 / 묶음 전체를 대표하는 단위 등>` | `<한 줄 책임>` | `<아래 "규칙" 표의 ID 참조>` |

### 규칙 (불변식)

항상 참이어야 하는 제약. 위반 시 어떻게 막는지도 함께 적는다.

| ID | 규칙(항상 참이어야 하는 것) | 위반 시 처리 | 관련 요구사항 |
| --- | --- | --- | --- |
| `INV-<영역>-001` | `<예: "상태 X에서는 필드 Y가 비어 있을 수 없다">` | `<거부/에러/자동 보정 등>` | `REQ-<영역>-001` |

### 이벤트

이 영역에서 발생하고, 다른 영역이 구독할 수 있는 사실.

| 이벤트 이름 | 발생 조건 | 페이로드(핵심 필드) | 구독자(알려진 경우) |
| --- | --- | --- | --- |
| `<Something Happened>` | `<무엇이 끝나면 발생하는지>` | `<필드1>`, `<필드2>` | `<영역 B>` |

### 식별자

| 식별 대상 | 식별자 형태 | 생성 시점/방식 | 비고 |
| --- | --- | --- | --- |
| `<블록 이름>` | `<예: 저장소 발급 일련값 / 클라이언트 생성 고유값 등>` | `<생성 주체와 시점>` | `<재사용/불변 여부>` |

## 영역: `<영역 B>`

`<위와 동일한 4개 하위 절(빌딩블록/규칙/이벤트/식별자)을 영역마다 반복>`

## 혼동되기 쉬운 규칙

이 전술 설계에서 이름이나 개념이 헷갈리는 규칙 쌍이 나오면 즉시 `boundary-catalog.md`에
"A vs B" 행으로 옮긴다. 여기에는 남기지 않는다.

## 변경 이력

| 버전 | 날짜 | 변경 | 사유 |
| --- | --- | --- | --- |
| `v0.1` | `<YYYY-MM-DD>` | 초안 작성 | `<...>` |
