# 백로그 — 열린 결정과 미배선 항목

- 상태: Active
- 최종 갱신: 2026-07-27 (하네스 부트스트랩 직후)

이 문서의 책임은 하나다: **"다음에 무엇을 해야 하는지"가 사람의 기억이나 지난 대화에만
남아 있지 않게 하는 것.** 항목이 해소되면 이 문서에서 지우고 해당 정본 문서(ADR ·
`docs/design/` · charter)에 결과를 남긴다.

세부 근거는 각 항목이 가리키는 문서에 있다. 여기서 중복 설명하지 않는다.

---

## 1. 슬라이스 진입 전 결정해야 하는 것

| # | 항목 | 왜 지금 아니면 늦는가 | 근거 |
| --- | --- | --- | --- |
| 1.1 | **인증 필터 방식** — `oauth2-resource-server` vs 커스텀 필터 | 첫 인증 슬라이스가 이 위에 올라간다. 나중에 바꾸면 필터 체인·에러 처리를 다시 짠다. `oauth2-resource-server`의 기본 401 바디와 `WWW-Authenticate` 헤더가 원본 RFC 7807 봉투와 **다르다** | `docs/design/spring-translation-map.md` §6 · charter §5.1 |
| 1.2 | **예외 클래스 이름** — `*Error`(원본 동일) vs `*Exception`(Java 관례) | 196개 에러가 이 규약을 따른다. 첫 슬라이스에서 정하지 않으면 섞인다 | 같은 문서 §6.3 |
| 1.3 | **Argon2 파라미터** — 원본 `argon2` 설정과 대조 | 전용 DB라 해시 호환은 불필요하지만 알고리즘·강도는 문서화해야 한다 | 같은 문서 §6 |

**1.1·1.2는 ADR로 남긴다** (`docs/adr/`). 트레이드오프가 있는 단일 결정이다.

## 2. 하네스 미배선 — 필요해지는 시점에

| # | 항목 | 언제 | 근거 |
| --- | --- | --- | --- |
| 2.1 | **OpenAPI 계약 diff 스크립트** | **첫 컨트롤러가 생길 때.** springdoc 의존성과 `openapi_3_0` 고정은 이미 들어갔지만, 생성된 스펙을 `upstream/tools/openapi/openapi.json`(82 path · 168 schema)과 비교하는 스크립트가 없다. 엔드포인트 0개인 지금 만들면 검증할 수 없다 | charter §5.1 |
| | ↳ **알려진 잔여 차이** | springdoc의 `OPENAPI_3_0`은 헤더를 `3.0.1`로 찍고 기준선은 `3.0.0`이다. `openapi` 필드 1줄은 허용 목록 대상 | `spring-translation-map.md` §6 |
| 2.2 | **로컬 인프라 compose** | 로컬에서 앱을 처음 띄울 때. `application-local.yaml`이 전용 Postgres `:15433` / Redis `:16380`을 가정하는데(원본 `:15432`/`:16379`와 겹치지 않게) 그 인스턴스를 띄우는 compose 파일이 없다 | charter §5.3 (전용 DB) |
| 2.3 | **Flyway V1 베이스라인** | 첫 엔티티가 생길 때. 핀된 `4.DB설계_Phase3.md` v1.1.5는 구현 마이그레이션과 전수 대조돼 **신뢰할 수 있는 입력**이다(23테이블, 유령 DDL 제거됨) | `spring-translation-map.md` §6.6 |
| 2.4 | **세션 레벨 가드 (선택)** | `.claude/settings.json`이 없어 Claude Code 세션 중 사전 차단은 없다. `.githooks/pre-commit`이 커밋 시점 하드 게이트라 필수는 아니다 | — |

## 3. upstream 핀 관련

| # | 항목 | 내용 | 근거 |
| --- | --- | --- | --- |
| 3.1 | **핀 이동은 자동이 아니다** | 원본 `docs/plan/`이 갱신돼도 이 저장소는 따라오지 않는다. `./scripts/upstream-sync.sh`로 확인하고, **diff를 읽은 뒤** `--pin <SHA>`로 올린다. 이건 해소 대상이 아니라 상시 운영 규칙이다 | `docs/upstream/UPSTREAM.md` §4 |

## 4. 슬라이스 안에서 처리할 것 (착수 시 게이트가 다시 꺼내준다)

| # | 항목 | 슬라이스 |
| --- | --- | --- |
| 4.1 | **BR-SM-012 파일 개수 상한을 명시적으로 잠근다** — 원본의 "우연한 보호"를 재현하지 않는다. 원본 코드 주석이 "채번을 바꾸면 행을 잠글 것"이라 지시하고 있고, JPA는 채번이 달라질 쪽이다. 동시 업로드 테스트 필수 | bc2 파일 업로드 |
| 4.2 | **도메인 VO의 Jackson 결합** — `AnnotationDocument`(256KB JSONB) 매핑 방식을 정하고, `ArchitectureRulesTest`의 선택적 규칙 `domain은_Jackson을_모른다`를 켤지 결정 | 주석(annotation) |
| 4.3 | **VO 승격은 선택적** — 31종 명세 ≠ 31클래스. 기준은 "값 자체에 로직이 있는가". 빈 껍데기 VO를 만들지 않는다 | 전 슬라이스 공통 |

## 5. 재구축 순서 (charter §7)

1. ~~프로젝트 골격과 계약 검증 하네스~~ — 골격 완료. 계약 diff는 2.1로 이관
2. **최소 인증 기반** ← 다음
3. Team BC 수직 슬라이스와 첫 Transactional Outbox 흐름
4. Sheet Music BC와 파일 저장소
5. Setlist BC와 스냅샷·동시성 로직
6. Notification BC와 Web Push
7. 인증 하드닝, 운영 작업 및 전체 계약 회귀

---

## 갱신 규칙

- 항목이 해소되면 **여기서 지우고** 결과를 정본 문서에 남긴다. 완료 목록을 여기 쌓지 않는다.
- 새 열린 결정이 생기면 `docs/design/spring-translation-map.md` §6 표에 행을 추가하고,
  "슬라이스 진입을 막는가"에 해당하면 여기 §1에도 올린다.
- 슬라이스 착수 시 `planning-gate` 스킬이 상위 진실을 재독하게 만든다. 이 문서는 그
  게이트의 입력이 아니라 **"무엇을 아직 안 했나"의 목록**이다.
